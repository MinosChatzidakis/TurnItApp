//paths to each page
export const ROUTES = {
  SPLASH: "/",
  JOIN_SESSION: "/join-session",
  HOST_SESSION: "/host-session",
  SUGGEST_SONGS: "/suggest-songs",
  HOST_DASHBOARD: "/host-dashboard",
};
//default paths to each page. If code are given, they are appended at the end of the path. Ability to receive code is declared in App.jsx. Handling of code happens in each page's jsx file
